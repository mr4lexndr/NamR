# clipper2-js declares @angular/{core,common} as peers purely because it is
# packaged with ng-packagr; its bundle imports neither. Installing those peers
# would add 42MB and three high-severity advisories for dead weight.
legacy-peer-deps=true
